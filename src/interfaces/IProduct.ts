export default interface IProduct {
  id: number;
  productRef: string;
  productImage: string;
  productName: string;
  productPrice: number;
  productDesc: string;
  productStock: string;
  available: boolean;
}
